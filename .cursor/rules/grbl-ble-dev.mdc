---
description: Grbl_Esp32 BLE/SPP project rules for minimal, verifiable, low-risk changes.
alwaysApply: true
---

# Grbl_Esp32 BLE/SPP 开发规则

适用范围：`Grbl_Esp32/src` 下与通信链路相关的改动（UART / BT / BLE / WebUI 入口）。

## 1) 先保稳定，再加功能

- 优先保持现有 `Grbl` 串口行为一致：命令输入路径、实时命令处理、`ok/error` 回包格式不变。
- 不在同一任务里同时做“功能新增 + 大重构”；新增 BLE 时只改必要文件。
- 未被需求点名的模块不顺手优化、不统一格式、不清理历史代码。

## 2) BLE 与经典蓝牙并存约束

- 若启用 BLE，保持与 `BluetoothSerial` 共存，不破坏现有 SPP 能力。
- 初始化顺序必须可解释：先确保经典蓝牙控制器可用，再启动 BLE GATT。
- 不随意调用会影响共享控制器生命周期的全局反初始化逻辑。
- BLE 默认按安全小包发送（例如 20 字节分片），避免在未协商 MTU 时丢包。

## 3) 只做外科式改动

- 优先在现有 client 抽象上扩展（`CLIENT_*`、`clientCheckTask`、`client_write`），避免平行新架构。
- 变更应能直接追溯到需求（例如“关闭 WiFi”“新增 BLE NUS”）。
- 若发现无关问题，记录给用户，不在当前任务内扩散处理。

## 4) 目标驱动验证（每次改动都要）

最少验证清单：

1. 编译通过：`platformio run -e release`
2. 启动日志包含通信初始化成功信息（BT/BLE）
3. 基础链路验证：
   - USB 串口可收发
   - 经典蓝牙 SPP 可收发
   - BLE 可写入命令并收到通知回包
4. 若关闭 WiFi：确认 WiFi 相关服务未启动且不影响 BT/BLE

## 5) 交付要求

- 汇报内容包含：改了哪些文件、为什么这样改、如何验证、未覆盖的风险点。
- 无法本地验证时，明确给出用户可直接执行的验证步骤与预期现象。
